# Reference repos (nzbdav, Radarr, Sonarr) — cloned locally for study/vendoring, not part of this repo
refs/

# .NET
bin/
obj/
*.user
.vs/
artifacts/

# Node
node_modules/
dist/
web/dist/
*.tsbuildinfo

# Built SPA copied into the server's web root for the single-origin prod path (Docker/CI)
server/src/Streamarr.Server/wwwroot/

# Local state
*.db
*.db-shm
*.db-wal
.env
.env.*
!.env.example
appsettings.Local.json
appsettings.*.Local.json
secrets.json
*.pfx
*.p12
*.pem
*.key

# Data Protection key ring (secret encryption at rest)
server/src/Streamarr.Server/keys/
keys/

# OS
.DS_Store

# Test output
TestResults/
playwright-report/
test-results/
coverage/
